[pytest]
# Human-readable test output — shows each test name and PASS/FAIL result
addopts = -v

# Where pytest should look for tests
testpaths = tests

# File name pattern pytest recognises as a test file
python_files = test_*.py

# Function/method name pattern pytest recognises as a test
python_functions = test_*
